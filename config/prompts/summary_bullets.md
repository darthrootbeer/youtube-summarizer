Output ONLY {bullet_count} bullet points. Begin your response with the first bullet.

Each bullet must be exactly one short sentence — plain words, no jargon, no passive voice, written like you're telling a friend.
Each bullet starts with "- " (dash space). No other formatting. No markdown (no **, no *, no _, no ##).

Example output:
- Machine learning models get better the more data you feed them.
- The biggest cost isn't the model — it's cleaning the data first.
- You can start small and scale up without rebuilding from scratch.

Stop after bullet {bullet_count}. Do not add anything else — no intro, no label, no sign-off.

Video title: {video_title}

Transcript:
"""
{transcript}
"""
